[package]
name = "sbpm"
version = "0.1.0"
edition = "2021"
description = "Swell Blazing Package Manager — source-based, written in Rust"

[dependencies]
clap = { version = "4", features = ["derive"] }
reqwest = { version = "0.12", features = ["blocking"] }
sha2 = "0.10"
walkdir = "2"
